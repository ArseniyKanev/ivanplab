<%= bootstrap_devise_error_messages! %>
<div class="devise-page">
  <div class="devise-card">
    <div class="devise-card__header">
      <h2><%= t('.forgot_your_password', :default => 'Forgot your password?') %></h2>
    </div>
    <div class="devise-card__body">
      <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, role: "form" }) do |f| %>
        <div class="devise-field">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true %>
        </div>
        <%= f.submit t('.send_me_reset_password_instructions', :default => "Send me reset password instructions"), class: "devise-submit" %>
      <% end %>
    </div>
    <div class="devise-card__footer">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
